# use the "config." prefix for any symbols to be delivered to the application

config.stackbase = "/lsst/DC3/distrib/w12"
config.serverdir = config.stackbase + "/www"
config.defeupstag = "current"
config.usebuildthreads = 4
